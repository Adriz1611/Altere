"use client";

import Image from "next/image";
import React from "react";
import {motion} from "framer-motion";

export default function Home() {
    return (<main className="py-32">
        <section id="hero" className="flex flex-row justify-between px-20 py-20">
            <h1 className="text-[4rem] font-medium w-2/4 font-main ">Waste is a Resource in the wrong place</h1>
        </section>
    </main>);
}
